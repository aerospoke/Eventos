import React from "react";
import styled from "styled-components";

const EventContainer = styled.div.attrs({
    className:'ma3 w-100 flex flex-column items-center'
})``

export default EventContainer